//1 
/**
 * Define a program that reads (using the scanf function) a sequence of integers ending with the number
0 and prints the largest element of the sequence on the screen.
 * 
 */

 #include <stdio.h>

 int um(){
    int maior;
    int num;
    while(maior ){scanf("Indique um num %d", &num);}
    if (num > maior){
        maior = num;
    }else{
        printf("O maior num é %d", maior);
    }
    return 0;
 }


//2 para fazer a média é a mesma coisa mas vou adicionando os valores a uma variavel soma e incremento ao num total e depois faço a divisão

//3 criar duas variaveis para guardar valores de maior e segundo maior e ir comparando os valores e trocando se for o caso

